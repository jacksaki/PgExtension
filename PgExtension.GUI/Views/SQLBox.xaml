<UserControl
    x:Class="PgExtension.GUI.Views.SQLBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mahApps="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:v="clr-namespace:PgExtension.GUI.Views"
    xmlns:vm="clr-namespace:PgExtension.GUI.ViewModels"
    xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBar>
            <Button
                Command="{Binding ExecuteCommand}"
                Content="{wpf:PackIcon Kind=LightningBolt}"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}" />
            <Button
                Command="{Binding BeautifyCommand}"
                Content="{wpf:PackIcon Kind=FormatKerning}"
                Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}" />
        </ToolBar>
        <Border
            Grid.Row="1"
            Margin="3"
            BorderBrush="Black"
            BorderThickness="1">
            <avalon:TextEditor
                Margin="4"
                Document="{Binding SQLDocument}"
                FontFamily="BIZ UDGothic"
                FontSize="22"
                ShowLineNumbers="True"
                SyntaxHighlighting="TSQL" />
        </Border>
    </Grid>
</UserControl>
